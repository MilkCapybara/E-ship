<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fandesunstar.eship.mapper.LoginLogMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.fandesunstar.eship.entity.LoginLog">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="username" property="username"/>
        <result column="ip_address" property="ipAddress"/>
        <result column="login_time" property="loginTime"/>
        <result column="status" property="status"/>
        <result column="fail_reason" property="failReason"/>
    </resultMap>

    <!-- 查询用户的登录日志 -->
    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT * FROM tb_login_log
        WHERE user_id = #{userId}
        ORDER BY login_time DESC
        <if test="limit != null">
            LIMIT #{limit}
        </if>
    </select>

    <!-- 统计用户登录失败次数（最近1小时内） -->
    <select id="countRecentFailedAttempts" resultType="java.lang.Long">
        SELECT COUNT(*) FROM tb_login_log
        WHERE username = #{username}
          AND status = 'FAILED'
          AND login_time &gt; CURRENT_TIMESTAMP - INTERVAL '#{hours} hours'
    </select>

</mapper>
